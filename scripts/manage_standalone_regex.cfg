# FTS process patterns to check for conflicts before running standalone scripts
# One pattern per line, OR-joined for pgrep -f matching
gen_fts_coverage.sh
build_fts_env.sh
special_tests.sh
python -m (coverage|pytest)
